const nextConfig = {
    images: {
        unoptimized: true,
    },
    trailingSlash: true,
    // basePath: "/portfolio-nextjs",
    // assetPrefix: "/portfolio-nextjs/",
};

export default nextConfig;