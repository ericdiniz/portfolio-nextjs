const nextConfig = {
    output: "export",
    images: {
        unoptimized: true,
    },
    trailingSlash: true,
    // basePath: "/portfolio-nextjs",
    // assetPrefix: "/portfolio-nextjs/",
};

export default nextConfig;